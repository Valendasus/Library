# 📚 Backend - Система Управления Электронной Библиотекой

Backend часть приложения на Express.js с REST API.

## 🚀 Установка

```bash
npm install
```

## 🏃 Запуск

```bash
npm start
```

Сервер запустится на `http://localhost:3000`

## 📡 API Endpoints

### Книги

- `GET /api/books` - Получить все книги
- `GET /api/books/:id` - Получить книгу по ID
- `POST /api/books` - Добавить новую книгу
- `PUT /api/books/:id` - Обновить книгу
- `DELETE /api/books/:id` - Удалить книгу
- `PATCH /api/books/:id/status` - Изменить статус книги

### Поиск и статистика

- `GET /api/search?query=...` - Поиск книг
- `GET /api/stats` - Получить статистику

## 🧪 Тестирование

### Получить все книги:
```bash
curl http://localhost:3000/api/books
```

### Добавить книгу:
```bash
curl -X POST http://localhost:3000/api/books \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Анна Каренина",
    "author": "Лев Толстой",
    "year": 1877,
    "category": "Классическая литература"
  }'
```

### Получить статистику:
```bash
curl http://localhost:3000/api/stats
```

## 🔧 Технологии

- Express.js - веб-фреймворк
- CORS - обработка кросс-доменных запросов
- Body-Parser - парсинг JSON

## 📝 Примечания

- Данные хранятся в памяти
- Frontend файлы обслуживаются из папки `../frontend`
