# CI/CD Pipeline для Library Platform

## 🚀 Автоматизация

### Этапы CI/CD:

1. **Test Backend** - проверка кода и зависимостей
2. **Build** - сборка всех Docker образов
3. **Integration Test** - тестирование API endpoints
4. **Deploy Local** - автоматический деплой на локальную машину

### 🔄 Триггеры:

- `push` в `main` или `develop` → запуск полного пайплайна
- `pull_request` в `main` → только тесты и сборка
- Деплой происходит только при `push` в `main`

---

## 🤖 Настройка локального деплоя

### Шаг 1: Установка Self-Hosted Runner

```bash
./deployment/setup-runner.sh
```

Это откроет инструкции для настройки GitHub Actions runner на вашей машине.

### Шаг 2: Настройка в GitHub

1. Откройте https://github.com/Valendasus/Library
2. Settings → Actions → Runners
3. New self-hosted runner
4. Выберите macOS
5. Следуйте инструкциям для загрузки и настройки

### Шаг 3: Запуск Runner

```bash
cd ~/actions-runner
./run.sh
```

Runner будет ожидать jobs от GitHub Actions.

---

## 📊 Что происходит при деплое:

1. ✅ Код проходит все тесты
2. 🔨 Собираются Docker образы
3. 🧪 Проверяются API endpoints
4. 🚀 Автоматически деплоится на локальную машину через Docker Swarm
5. ✅ Проверяется доступность приложения

---

## 🧪 Тесты API

Pipeline автоматически тестирует:
- `GET /api/stats` - статистика
- `GET /api/books` - список книг
- `POST /api/books` - добавление книги

---

## 🎯 Результат

После успешного деплоя:
- 🌐 Приложение доступно на http://localhost
- 📊 7 реплик в Docker Swarm (3 backend + 2 frontend)
- ✅ Автоматический rollback при ошибках

---

## 📝 Мониторинг

Смотреть логи деплоя:
- GitHub Actions → вкладка Actions в репозитории
- Локально: `docker stack services library`

---

## 🔧 Troubleshooting

**Runner не подключается:**
```bash
cd ~/actions-runner
./config.sh remove
# Затем заново configure
```

**Деплой не запускается:**
- Проверьте что runner запущен: `./run.sh`
- Проверьте что он онлайн в Settings → Actions → Runners

**Порт 80 занят:**
```bash
docker stack rm library
# Или остановите другие сервисы на порту 80
```
