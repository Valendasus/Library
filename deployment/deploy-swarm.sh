#!/bin/bash

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🚀 Развёртывание кластеризованной библиотеки в Docker Swarm
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

set -e

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🏛️  РАЗВЁРТЫВАНИЕ КЛАСТЕРИЗОВАННОЙ ПЛАТФОРМЫ"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# Проверка Docker Swarm
if ! docker info 2>/dev/null | grep -q "Swarm: active"; then
    echo "⚠️  Docker Swarm не активен. Инициализация..."
    docker swarm init
    echo "✅ Docker Swarm инициализирован"
fi

# Сборка образов
echo ""
echo "📦 Сборка Docker образов..."
docker-compose -f docker-compose.yml build --parallel

# Развёртывание stack
echo ""
echo "🚀 Развёртывание stack в Swarm..."
docker stack deploy -c docker-compose.swarm.yml library

# Ожидание запуска
echo ""
echo "⏳ Ожидание запуска сервисов..."
sleep 5

# Статус сервисов
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📊 СТАТУС СЕРВИСОВ:"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
docker stack services library

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ Развёртывание завершено!"
echo ""
echo "🌐 Приложение доступно: http://localhost"
echo ""
echo "📋 Полезные команды:"
echo "   docker stack ps library          - статус задач"
echo "   docker service logs library_backend -f  - логи backend"
echo "   docker service scale library_backend=5  - масштабирование"
echo "   docker stack rm library          - удаление stack"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
